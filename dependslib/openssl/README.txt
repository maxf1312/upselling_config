1. Build openssl as static library and instal it into some folder (i.e. c:\lib\installed\openssl)
2. Copy include and lib folders info include and lib respectively
3. If dll version was built and used - copy from lib to implib needed files